/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // output: 'export',
  // distDir: "build",
  trailingSlash:true

}

module.exports = nextConfig
